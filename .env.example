# ═══ Ceptra AI — Configuration ═══

# ── Cloud AI (for Vercel deployment) ──
OPENROUTER_API_KEY=sk-or-v1-your-key-here
# OPENROUTER_MODEL=qwen/qwen-2.5-72b-instruct  # Default. Other options:
# OPENROUTER_MODEL=anthropic/claude-3.5-sonnet   # Best quality ($3/M tokens)
# OPENROUTER_MODEL=meta-llama/llama-3.3-70b      # Great + cheap ($0.40/M)
# OPENROUTER_MODEL=google/gemini-2.0-flash       # Fastest ($0.10/M)

# ── Local AI (optional — only if running Ollama locally) ──
# OLLAMA_URL=http://localhost:11434
# DEFAULT_MODEL=qwen2.5:7b

# ── Optional: Anthropic direct (better quality, higher cost) ──
# ANTHROPIC_API_KEY=sk-ant-...

# ── App ──
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
